name: Wheels

# Reusable: the release workflow calls this to build what it publishes, and a
# manual run builds the same wheels without publishing them.

on:
  workflow_call:
    inputs:
      cibw-build:
        description: Interpreters to build, as a CIBW_BUILD pattern.
        type: string
        required: true
  workflow_dispatch:
    inputs:
      cibw-build:
        description: Interpreters to build, as a CIBW_BUILD pattern.
        type: string
        default: "cp310-* cp311-* cp312-* cp313-*"

jobs:
  wheels:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-14]
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0 # setuptools_scm reads the tags

      - uses: pypa/cibuildwheel@v4.2.1
        env:
          CIBW_BUILD: ${{ inputs.cibw-build }}
          CIBW_SKIP: "*-musllinux_*"
          CIBW_ARCHS_LINUX: x86_64
          CIBW_ARCHS_MACOS: arm64
          # Loads the compiled extension, not only the package.
          CIBW_TEST_COMMAND: "python -c \"from pulserver._accelerators import require; print(require().__name__)\""

      - uses: actions/upload-artifact@v7
        with:
          name: wheels-${{ matrix.os }}
          path: wheelhouse/*.whl
          if-no-files-found: error

  sdist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - run: |
          python -m pip install --upgrade pip build
          python -m build --sdist
      - uses: actions/upload-artifact@v7
        with:
          name: sdist
          path: dist/*.tar.gz
          if-no-files-found: error
