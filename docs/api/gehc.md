# `pulserver.gehc`

`pulserver.gehc` is the gateway to the separately distributed, private
`pulserver-gehc` package. Its scanner-specific API is not published by this
repository yet. Install the GE extension package before importing this
namespace.
