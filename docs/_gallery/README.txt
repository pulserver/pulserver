Python examples
===============

Longer worked material than a docstring holds: end-to-end runs a reader can
follow, with the code that produced every number and picture. The sequence and
reconstruction plugins are not here -- they are shipped code, documented on the
:doc:`apps page </api/python/apps>`.
