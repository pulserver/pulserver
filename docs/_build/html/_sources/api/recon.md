# `pulserver.recon`

Reconstruction-server APIs are intentionally left as a namespace stub for
now. They depend on the optional `pulserver[recon]` dependencies and will gain
their own grouped reference once the public recon boundary is formalised.
