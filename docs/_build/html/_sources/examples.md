# Examples

The sequence plugins in
[`examples/sequences/`](https://github.com/INFN-MRI/pulserver/tree/main/examples/sequences)
are complete examples. API pages include compact, runnable examples; where an
example produces a figure, the plotting implementation is hidden and the
result is shown directly in the documentation.

```{toctree}
:hidden:

api/pypulseq
```
