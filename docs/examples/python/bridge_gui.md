# Driving a sequence from the GUI and the console

A plugin written once, exercised three ways: through nimpulseqgui's property
editor, through `--validate-only` for a single answer, and through the
persistent loop a console keeps warm.

```{note}
This page is being written.
```
