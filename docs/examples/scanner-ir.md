# Scanner IR

The segmented representation of a sequence that a scanner interpreter plays.

| Page | What it establishes |
| --- | --- |
| {doc}`/generated/gallery/02-scanner-ir/01_segmentation` | The repeating unit and segments of a 2D spin echo, the independence of the segment count from the scan length, and the subsequences of a chain with a prescan. |

```{toctree}
:hidden:

/generated/gallery/02-scanner-ir/01_segmentation
```
