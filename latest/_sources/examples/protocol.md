# Protocol resolution

The protocol a scanner sequence resolves a prescription to, and the design
limits that determine it.

| Page | What it establishes |
| --- | --- |
| {doc}`/generated/gallery/01-protocol/01_protocol_resolution` | How the sampling rasters quantize the receiver bandwidth of a 2D gradient echo, how the shortest echo time depends on matrix size and bandwidth, and how an infeasible request is reported. |

```{toctree}
:hidden:

/generated/gallery/01-protocol/01_protocol_resolution
```
