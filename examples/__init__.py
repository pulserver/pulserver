"""Standalone Pulserver example modules.

The sequence modules are packaged as ``pulserver.sequences`` by
``pyproject.toml``.  This package marker also makes the same modules directly
importable from an uninstalled source checkout.
"""
